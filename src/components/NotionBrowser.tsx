"use client";

// TODO: 자주 쓰는 태스크 목록 표시 예정
import { EmptyState } from "@/components/EmptyState";

export function NotionBrowser() {
  return (
    <EmptyState className="text-text-dim">— Saved tasks (coming soon) —</EmptyState>
  );
}
